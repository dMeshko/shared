import { FluentFormComponentControlDirective } from './fluent-form-component-control.directive';

describe('FluentFormComponentControlDirective', () => {
  it('should create an instance', () => {
    //const directive = new FluentFormComponentControlDirective();
    //expect(directive).toBeTruthy();
  });
});
