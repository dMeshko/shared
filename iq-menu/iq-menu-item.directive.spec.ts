import { IqMenuTemplateDirective } from './iq-menu-template.directive';

describe('IqMenuItemDirective', () => {
  it('should create an instance', () => {
    //const directive = new IqMenuTemplateDirective();
    //expect(directive).toBeTruthy();
  });
});
